# fsp-quality-of-life

Userscript for fsp.fpt.edu.vn / edunext. Built with Svelte + TypeScript + Vite + vite-plugin-monkey.

## Agent skills

### Issue tracker

Issues live in GitHub Issues (`thetommylong/fsp`), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
